带本地存储的即时贴-大量数据不要集中存放在同一本地key中，每次操作JSON转换会有很大负担-

jq.html() innerHTML
jq.val() value
jq.text() txt

key: localStorage.key(i) for (var keyv in localStorage){var kz = keyv}
value:localStorage.getItem(key)=localStorage[key]=localStorage.key

obj.value=obj[value]